numerator = float(input("Enter the numerator value: "))
denominator = float(input("Enter the denominator value: "))

percentage = (numerator / denominator) * 100

print(numerator,"is", percentage,"%" ,"of", denominator)